# Dwar provider keys. Copy to .env and fill in when you need LLM/embed/speech.
# Empty is fine for bring-up — Dwar boots and logs; provider routes return 503 until set.
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
